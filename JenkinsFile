pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello World"'
                sh '''
                    echo "Multiline shell steps works too"
                    cd /Users/cramapra/ddrive/NextGen/helidon-quickstart-mp-extended
                    mvn package
                '''
            }
        }
    }
}
